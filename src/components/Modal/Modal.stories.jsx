import React, { Component } from 'react';
import Modal from './index';

export default {
    title: 'Event Meet/ModalPopUp',
    component: Modal,
    args: {

    }
}

export const All = (args) => <Modal {...args} />
